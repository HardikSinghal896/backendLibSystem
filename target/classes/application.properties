#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASS}
#spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/library_db
spring.datasource.username=hardik
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver


spring.application.name=Seth Kanhaiya Lal Sethiya - Bhatia Ashram Library

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

spring.jackson.serialization.write-dates-as-timestamps=false

# Admin credentials ? set these as env vars on Render
admin.email=${ADMIN_EMAIL:admin@library.com}
admin.password=${ADMIN_PASSWORD:admin123}

server.port=${PORT:8080}

# Cloudinary ? set these as env vars on Render
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:diq2ka93d}
cloudinary.api-key=${CLOUDINARY_API_KEY:921766788957938}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:cY2yTu8Eqj5TSXRVn2aqITMo0WM}

# Allow multipart uploads up to 10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB